# My Laravel Rivers Demo

## Install

1. Clone the repo
2. Migrate && seed the database
    ```shell
   php artisan migrate:fresh --seed
    ```

## Usage
1. Run the queue
    ```shell
    composer work
    ```
2. Visit the site and try it out
